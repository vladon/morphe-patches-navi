# Скопируй в файл `gradle.properties` в этой же папке (файл уже в .gitignore — в git не попадёт).
# Значения возьми из GitHub — см. README.md раздел «GitHub».

gpr.user=ВСТАВЬ_СВОЙ_ЛОГИН_GITHUB
gpr.key=ВСТАВЬ_СЮДА_PAT_КАК_ОДНУ_СТРОКУ_БЕЗ_КАВЫЧЕК
