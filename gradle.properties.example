# Скопируй этот файл как `gradle.properties` в корень проекта (локальный файл в .gitignore — не попадёт в git).

org.gradle.parallel=true
org.gradle.caching=true
kotlin.code.style=official
version=1.0.0

# GitHub Packages (Morphe) — см. README.md
gpr.user=ВСТАВЬ_СВОЙ_ЛОГИН_GITHUB
gpr.key=ВСТАВЬ_СЮДА_PAT_КАК_ОДНУ_СТРОКУ_БЕЗ_КАВЫЧЕК
